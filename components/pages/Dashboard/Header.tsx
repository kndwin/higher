import { Container, Group, Text } from "components/shared";

export const Header = () => {
  return (
    <Container.Header>
      <Group>
				<Text>{`Hello world`}</Text>
			</Group>
    </Container.Header>
  );
};
